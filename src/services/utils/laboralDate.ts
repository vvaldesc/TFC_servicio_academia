// import { MORNING_BOOKING_SCHEDULE, AFTERNOON_BOOKING_SCHEDULE } from '@/consts/consts';

// export default function laboralDate(date: Date) {
//     const hour = date.getHours();
//     return (
//     hour !== MORNING_BOOKING_SCHEDULE.start
//     && hour !== MORNING_BOOKING_SCHEDULE.end
//     && hour >= AFTERNOON_BOOKING_SCHEDULE.start
//     && hour <= AFTERNOON_BOOKING_SCHEDULE.end);
// }